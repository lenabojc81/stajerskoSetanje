import React from "react";
import { Text, Button } from "react-native";

const PremikDoZacetneLokacije = () => {

    return (
        <>
        <Text>Premik do začetne lokacije: Premaknite se do te lokacije, da začnete z igro.</Text>
        </>
    );
};

export default PremikDoZacetneLokacije;